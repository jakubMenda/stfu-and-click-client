import React from 'react'
import { Wrapper } from './styled'

interface Props {}

const Footer = (props: Props) => {
  return <Wrapper>Want to be on top? Just STFU and click!</Wrapper>
}

export default Footer
